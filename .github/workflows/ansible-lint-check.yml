name: Ansible Lint check

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      
    - name: Lint Playbook gw01.ffbsee.yml
      uses: ansible/ansible-lint-action@master
      with:
        targets: gw01.ffbsee.yml

    - name: Lint Playbook gw02.ffbsee.yml
      uses: ansible/ansible-lint-action@master
      with:
        targets: gw02.ffbsee.yml

    - name: Lint Playbook gw03.ffbsee.yml
      uses: ansible/ansible-lint-action@master
      with:
        targets: gw03.ffbsee.yml

    - name: Lint Playbook gw04.ffbsee.yml
      uses: ansible/ansible-lint-action@master
      with:
        targets: gw04.ffbsee.yml
